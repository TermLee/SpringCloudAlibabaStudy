server:
  port: 3377

spring:
  application:
    name: nacos-config-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml

# 服务名 - profiles . file-extension
# 对应的配置文件名: nacos-config-server-dev.yml